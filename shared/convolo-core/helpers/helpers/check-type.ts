/**
 * checks type of some value inline and return as is
 * good if need to check type without additional variable
 * also can be used to set any type
 */
export const checkType = <Type>(value: Type): Type => value;
